# Headers added to API responses (helmet in the API sets CSP, X-Frame-Options,
# nosniff etc.; the API echoes X-Request-Id itself).
add_header Strict-Transport-Security $cbi_hsts always;
add_header X-Robots-Tag "noindex, nofollow" always;
